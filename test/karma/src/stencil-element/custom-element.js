import { ensureFeatures, h, StencilElement } from '../../../../dist/index';


defineHostElement(window, [], (scopedCss, mode) => {

  if (scopedCss) {
    return import('../asdf.sc.js');
  }

  if (scopedCss) {
    return import('../asdf.sc.js');
  }

  return import('../asdf.sc.js');

});